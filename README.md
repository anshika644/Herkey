# HerFuture Salary Calculator

Single-page salary calculator for the HerFuture × Simplilearn × HerKey partnership offer (₹10,000 vs typical ₹40,000 market pricing).

## Run locally

- Open `index.html` directly in a browser (no build step).
- Or serve the folder with any static server (optional).
